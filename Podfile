# Podfile for StampCollection app
# This should be very simple for now, I only want to use SwiftSoup
# UPDATE 12/28/23: Using SwiftSoup via Swift Package Manager now

use_frameworks!

target 'StampCollection' do
#    pod 'TMCache'
#    pod 'CHCSVParser'
end
#post_install do |installer|
#  installer.pods_project.targets.each do |target|
#    target.build_configurations.each do |config|
#      # some older pods don't support some architectures, anything over IOS 11 resolves that
#      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '12ß.0'
#    end
#  end
#end
